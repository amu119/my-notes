# 网站基本信息（使用 SVG 图标替换文字）
title = "鸿鹄"

# 模块配置
[module]
[[module.imports]]
disable = false
ignoreConfig = false
ignoreImports = false
path = 'github.com/gohugo-ananke/ananke/v2'

# 菜单配置
[[menu.main]]
url = "/"
weight = 1
identifier = "home"

[[menu.main]]
url = "/posts/"
weight = 2
identifier = "posts"

[[menu.main]]
url = "javascript:void(0)"
weight = 3
identifier = "search"
# 配置首页输出格式，必须包含 JSON
[outputs]
  home = ["HTML", "RSS", "JSON"]

# 主题参数配置
[params]
  mainSections = ["posts"]